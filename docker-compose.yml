services:
  api:
    build: .
    ports:
      - "4343:4343"
    env_file:
      - .env
    environment:
      - SERVICE=api

  bot:
    build: .
    env_file:
      - .env
    environment:
      - SERVICE=bot
    depends_on:
      - api 